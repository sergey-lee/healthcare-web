# 📝 Как изменять текст на сайте

## ⚠️ ВАЖНО: Почему текст не меняется

Вы столкнулись с проблемой: текст меняется только при редактировании HTML файлов, а не JSON. Вот почему:

### Как работает i18n система:

```
HTML файл                    i18n-loader.js              i18n_categorized.json
┌─────────────┐             ┌──────────────┐            ┌─────────────────┐
│             │             │              │            │                 │
│ <button     │   ─────>    │  Находит все │  ─────>    │ "navigation": { │
│  data-i18n= │             │  элементы с  │            │   "about":      │
│  "nav.about"│             │  data-i18n   │            │   "О нас"       │
│ >           │             │              │            │ }               │
│   About     │   <─────    │  Заменяет    │  <─────    │                 │
│ </button>   │             │  текст из    │            │                 │
│             │             │  JSON        │            │                 │
└─────────────┘             └──────────────┘            └─────────────────┘
```

**Ключевой момент:** i18n-loader.js работает в **браузере**, не на диске!

---

## 🚀 Решение: Запустите локальный веб-сервер

### Вариант 1: Используйте готовый скрипт (Рекомендуется)

```bash
cd /Users/meditor/Projects/healthcare-web
./start-server.sh
```

Затем откройте в браузере:
- **Тестовая страница:** http://localhost:8000/test-i18n.html
- **Главная страница:** http://localhost:8000/index.html

### Вариант 2: Запустите сервер вручную

```bash
cd /Users/meditor/Projects/healthcare-web
python3 -m http.server 8000
```

---

## 📝 Пошаговая инструкция

### Шаг 1: Запустите веб-сервер

В терминале:
```bash
cd /Users/meditor/Projects/healthcare-web
./start-server.sh
```

Вы увидите:
```
Запуск веб-сервера на http://localhost:8000
```

### Шаг 2: Откройте тестовую страницу

1. Откройте браузер
2. Перейдите по адресу: http://localhost:8000/test-i18n.html
3. Вы должны увидеть:
   - ✅ i18n загружен
   - ✅ JSON загружен (10 категорий)

### Шаг 3: Измените текст в JSON

1. Откройте `i18n_categorized.json` в редакторе
2. Найдите строку, которую хотите изменить:
   ```json
   {
     "strings": {
       "navigation": {
         "about": "About"
       }
     }
   }
   ```

3. Измените значение:
   ```json
   {
     "strings": {
       "navigation": {
         "about": "О НАС (ИЗМЕНЕНО!)"
       }
     }
   }
   ```

4. **СОХРАНИТЕ** файл (Cmd+S / Ctrl+S)

### Шаг 4: Обновите страницу в браузере

**Вариант A:** Жесткая перезагрузка (очистка кэша)
- **Mac:** Cmd + Shift + R
- **Windows/Linux:** Ctrl + Shift + R

**Вариант B:** Кнопка "Перезагрузить переводы" на тестовой странице

### Шаг 5: Проверьте изменения

Текст на странице должен измениться с "About" на "О НАС (ИЗМЕНЕНО!)"

---

## 🔍 Диагностика проблем

### Проблема: JSON файл read-only

Если VSCode показывает файл как read-only:

1. **Проверьте права:**
   ```bash
   ls -la i18n_categorized.json
   ```
   Должно быть: `-rw-r--r--`

2. **Удалите расширенные атрибуты:**
   ```bash
   xattr -c i18n_categorized.json
   ```

3. **Попробуйте другой редактор:**
   - Sublime Text
   - Atom
   - Обычный TextEdit (на Mac)

### Проблема: Ошибка 404 на JSON файл

В консоли браузера (F12) видите:
```
Failed to load /i18n_categorized.json: 404 Not Found
```

**Решение:** Проверьте путь к JSON в `i18n-loader.js`:

```javascript
// Строка 16
const response = await fetch('/i18n_categorized.json');
```

Если файл в другой директории, измените путь:
```javascript
const response = await fetch('/path/to/i18n_categorized.json');
```

### Проблема: CORS ошибка

Если видите:
```
Access to fetch at 'file:///...' has been blocked by CORS policy
```

**Причина:** Вы открыли HTML файл напрямую (`file:///`)

**Решение:** ОБЯЗАТЕЛЬНО используйте веб-сервер (см. выше)

### Проблема: Изменения не применяются

1. **Очистите кэш браузера:** Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)

2. **Проверьте консоль браузера (F12):**
   - Ищите сообщения `[i18n]`
   - Ищите ошибки (красные)

3. **Убедитесь, что JSON валидный:**
   ```bash
   python3 -c "import json; json.load(open('i18n_categorized.json'))"
   ```
   Не должно быть ошибок.

4. **Перезагрузите переводы вручную:**
   В консоли браузера:
   ```javascript
   window.i18n.reload()
   ```

---

## 💡 Быстрая проверка

### Тест 1: Скрипт загружен?

В консоли браузера (F12):
```javascript
window.i18n
```

Должно вывести объект с методами `getString`, `reload`, `enableObserver`.

### Тест 2: JSON загружен?

```javascript
window.i18n.getString('navigation.about')
```

Должно вернуть: `"About"` (или ваше измененное значение)

### Тест 3: Переводы применяются?

1. Откройте test-i18n.html
2. Нажмите кнопку "Применить переводы"
3. Текст должен измениться

---

## 📋 Чеклист

- [ ] Запущен веб-сервер (`./start-server.sh`)
- [ ] Открыта страница через http://localhost:8000
- [ ] В консоли видны сообщения `[i18n] Loaded translations`
- [ ] Файл i18n_categorized.json можно редактировать
- [ ] После изменения JSON делается Cmd+Shift+R
- [ ] Текст меняется на странице

---

## 🎯 Примеры изменения популярных строк

### Изменить кнопку "Search":

```json
{
  "strings": {
    "navigation": {
      "search": "Поиск"  // было "Search"
    }
  }
}
```

### Изменить "About":

```json
{
  "strings": {
    "navigation": {
      "about": "О нас"  // было "About"
    }
  }
}
```

### Изменить placeholder в форме:

```json
{
  "strings": {
    "forms": {
      "email": "Ваш email"  // было "이메일"
    }
  }
}
```

---

## ⚡ Быстрый старт (TL;DR)

```bash
# 1. Запустите сервер
cd /Users/meditor/Projects/healthcare-web
./start-server.sh

# 2. Откройте в браузере
# http://localhost:8000/test-i18n.html

# 3. Измените i18n_categorized.json
# Сохраните (Cmd+S)

# 4. Обновите браузер
# Cmd+Shift+R (Mac) или Ctrl+Shift+R (Windows)

# 5. Готово! 🎉
```

---

## 🆘 Если ничего не помогло

1. Откройте консоль браузера (F12)
2. Скопируйте все ошибки (красные)
3. Проверьте вкладку Network:
   - Найдите запрос к `i18n_categorized.json`
   - Какой статус? (должен быть 200)
   - Какой размер? (должен быть ~364 KB)

Если все еще не работает, возможно проблема в самом HTML - проверьте, что атрибуты `data-i18n` добавлены корректно.
