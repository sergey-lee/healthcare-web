# Руководство по интеграции i18n системы

## 🎉 Что было сделано

Все захардкоженные текстовые строки в вашем проекте были успешно заменены на систему интернационализации (i18n). Теперь вы можете легко изменять весь текст на сайте в одном месте!

### Статистика

- **Обработано файлов:** 73 HTML файла
- **Файлов с изменениями:** 64
- **Всего замен строк:** 15,236
- **Уникальных строк в словаре:** 483

---

## 📁 Созданные файлы

### 1. Основные файлы

| Файл | Описание | Размер |
|------|----------|--------|
| `i18n_categorized.json` | **Главный файл со всеми строками** (организованы по категориям) | 356 KB |
| `i18n-loader.js` | JavaScript загрузчик, который применяет переводы на страницах | 4 KB |
| `replace_hardcoded_strings.py` | Python скрипт для замены строк (уже выполнен) | 8 KB |

### 2. Дополнительные файлы

- `text_strings.json` - простой JSON со всеми строками
- `i18n_flat.json` - плоская структура с префиксами
- `text_strings.js` - ES6 модуль для JavaScript
- `i18n.d.ts` - TypeScript определения типов
- `example_usage.jsx` - примеры использования в React

---

## 🚀 Как это работает

### Шаг 1: Структура данных

Все строки хранятся в `i18n_categorized.json`:

```json
{
  "strings": {
    "navigation": {
      "about": "About",
      "faq": "FAQ",
      "search": "Search",
      ...
    },
    "content_en": {
      "welcome": "Welcome to our site",
      ...
    },
    "content_ko": {
      "gallery": "갤러리",
      ...
    },
    "forms": {
      "name": "성함",
      "email": "이메일",
      "phone": "전화번호",
      ...
    }
  }
}
```

### Шаг 2: HTML с data-i18n атрибутами

Все текстовые строки в HTML теперь имеют специальные атрибуты:

```html
<!-- Текстовый контент -->
<button data-i18n="buttons.search">Search</button>
<h1 data-i18n="navigation.about">About</h1>

<!-- Атрибуты -->
<input data-i18n-placeholder="forms.email" placeholder="이메일">
<img data-i18n-alt="content_en.logo" alt="mwpdemo44061">
<a data-i18n-title="navigation.home" title="Home">Link</a>
<button data-i18n-aria="navigation.menu" aria-label="Menu">☰</button>
```

### Шаг 3: JavaScript загрузчик

Файл `i18n-loader.js` автоматически:

1. Загружает `i18n_categorized.json`
2. Находит все элементы с `data-i18n*` атрибутами
3. Заменяет текст на соответствующие значения из JSON
4. Следит за новыми элементами (для динамического контента)

---

## ✅ Следующие шаги (требуется действие!)

### Шаг 1: Подключить i18n-loader.js

Вам нужно добавить загрузчик в ваши HTML шаблоны. Есть несколько вариантов:

#### Вариант A: Добавить в каждый HTML файл вручную

Добавьте перед закрывающим тегом `<script src="/i18n-loader.js" defer></script></body>`:

```html
<script></script>
<script src="/i18n-loader.js" defer></script></body>
</html>
```

#### Вариант B: Добавить в WordPress тему (Рекомендуется)

Если это WordPress сайт, добавьте в файл `wp-content/themes/salient-child/footer.php`:

```php
<?php wp_footer(); ?>
<script src="<?php echo get_stylesheet_directory_uri(); ?>/i18n-loader.js"></script>
<script src="/i18n-loader.js" defer></script></body>
</html>
```

И скопируйте файлы:

```bash
cp i18n-loader.js wp-content/themes/salient-child/
cp i18n_categorized.json wp-content/themes/salient-child/
```

Затем обновите путь в `i18n-loader.js` (строка 13):

```javascript
const response = await fetch('<?php echo get_stylesheet_directory_uri(); ?>/i18n_categorized.json');
```

### Шаг 2: Тестирование

1. Откройте сайт в браузере
2. Откройте консоль разработчика (F12)
3. Должны увидеть сообщения:
   ```
   [i18n] Initializing...
   [i18n] Loaded translations: 10 categories
   [i18n] Translations applied
   ```

4. Проверьте, что текст на странице корректно отображается

### Шаг 3: Изменение текста

Теперь чтобы изменить любой текст на сайте:

1. Откройте `i18n_categorized.json`
2. Найдите нужную строку (например: `"search": "Search"`)
3. Измените значение на нужное
4. Сохраните файл
5. Перезагрузите страницу в браузере

**Пример изменения:**

```json
{
  "strings": {
    "navigation": {
      "search": "Поиск",  // Было "Search", стало "Поиск"
      "about": "О нас"     // Было "About", стало "О нас"
    }
  }
}
```

---

## 🔧 Полезные команды

### Перезагрузить переводы без обновления страницы

Откройте консоль браузера и выполните:

```javascript
window.i18n.reload()
```

### Получить строку программно

```javascript
const searchText = window.i18n.getString('navigation.search')
console.log(searchText) // "Search"
```

---

## 📊 Категории строк

Ваши строки организованы в следующие категории:

| Категория | Количество | Описание |
|-----------|------------|----------|
| `content_en` | 238 | Английский контент |
| `contact` | 99 | Контактная информация |
| `content_ko` | 51 | Корейский контент |
| `navigation` | 28 | Меню и навигация |
| `research` | 25 | Научные исследования |
| `services` | 12 | Услуги |
| `buttons` | 9 | Кнопки |
| `forms` | 8 | Формы |
| `dates` | 7 | Даты |
| `company` | 6 | Информация о компании |

---

## 🌍 Добавление мультиязычности (опционально)

Если в будущем захотите добавить несколько языков:

### 1. Создайте файлы для каждого языка

```
i18n_categorized.en.json  // Английский
i18n_categorized.ko.json  // Корейский
i18n_categorized.ru.json  // Русский
```

### 2. Измените i18n-loader.js

```javascript
// Определить текущий язык
const currentLang = localStorage.getItem('language') || 'en';

// Загрузить соответствующий файл
const response = await fetch(`/i18n_categorized.${currentLang}.json`);
```

### 3. Добавьте переключатель языка

```html
<select onchange="changeLanguage(this.value)">
  <option value="en">English</option>
  <option value="ko">한국어</option>
  <option value="ru">Русский</option>
</select>

<script>
function changeLanguage(lang) {
  localStorage.setItem('language', lang);
  window.i18n.reload();
}
</script>
```

---

## 🐛 Решение проблем

### Проблема: Текст не меняется

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь, что `i18n-loader.js` загружен
3. Проверьте путь к `i18n_categorized.json`
4. Убедитесь, что элемент имеет атрибут `data-i18n`

### Проблема: JSON файл не загружается (404)

**Решение:**
1. Проверьте путь в `i18n-loader.js` (строка 13)
2. Убедитесь, что файл находится в правильной директории
3. Для WordPress используйте:
   ```javascript
   const response = await fetch('<?php echo get_stylesheet_directory_uri(); ?>/i18n_categorized.json');
   ```

### Проблема: Некоторые строки не переведены

**Решение:**
1. Проверьте, что элемент имеет правильный атрибут `data-i18n`
2. Проверьте, что ключ существует в JSON файле
3. Запустите скрипт заново:
   ```bash
   python3 replace_hardcoded_strings.py
   ```

---

## 📝 Примеры использования

### Добавить новую строку

1. Добавьте в `i18n_categorized.json`:
   ```json
   {
     "strings": {
       "buttons": {
         "submit": "Отправить"
       }
     }
   }
   ```

2. Используйте в HTML:
   ```html
   <button data-i18n="buttons.submit">Submit</button>
   ```

### Использовать в JavaScript

```javascript
// Дождитесь загрузки i18n
document.addEventListener('DOMContentLoaded', () => {
  setTimeout(() => {
    const submitText = window.i18n.getString('buttons.submit');
    console.log(submitText);
  }, 1000);
});
```

### Динамически добавленные элементы

```javascript
// Новые элементы с data-i18n будут автоматически обработаны
const newButton = document.createElement('button');
newButton.setAttribute('data-i18n', 'buttons.submit');
newButton.textContent = 'Submit'; // Временный текст
document.body.appendChild(newButton);

// i18n-loader автоматически обнаружит и применит перевод
```

---

## 🎯 Лучшие практики

1. **Всегда используйте осмысленные ключи**
   - ✅ Хорошо: `data-i18n="buttons.submit"`
   - ❌ Плохо: `data-i18n="btn1"`

2. **Группируйте строки по категориям**
   - Навигация → `navigation.*`
   - Формы → `forms.*`
   - Кнопки → `buttons.*`

3. **Храните исходный текст в HTML как резервный**
   ```html
   <button data-i18n="buttons.submit">Submit</button>
   ```
   Если i18n не загрузится, пользователь увидит "Submit"

4. **Используйте версионирование для JSON**
   ```javascript
   const response = await fetch('/i18n_categorized.json?v=1.0.0');
   ```

5. **Делайте резервные копии перед изменениями**
   ```bash
   cp i18n_categorized.json i18n_categorized.json.backup
   ```

---

## 📚 Дополнительные ресурсы

- [INDEX.md](INDEX.md) - навигация по всем созданным файлам
- [README_i18n.md](README_i18n.md) - детальная документация
- [TEXT_EXTRACTION_REPORT.md](TEXT_EXTRACTION_REPORT.md) - отчет об извлечении строк

---

## ✨ Готово!

Теперь ваш проект полностью готов к удобному управлению текстовым контентом. Все строки в одном месте, легко редактируются и готовы к мультиязычности!

**Следующие шаги:**
1. ☑️ Подключите `i18n-loader.js` в ваши шаблоны
2. ☑️ Протестируйте на нескольких страницах
3. ☑️ Попробуйте изменить несколько строк в JSON
4. ☑️ Сделайте commit в git

Удачи! 🚀
