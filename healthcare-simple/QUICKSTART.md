# 🚀 Быстрый старт

## Запуск за 30 секунд

### Вариант 1: Автоматический запуск (Рекомендуется)

```bash
cd healthcare-simple
./start-server.sh
```

Откройте браузер: http://localhost:8000

### Вариант 2: Python

```bash
cd healthcare-simple
python3 -m http.server 8000
```

### Вариант 3: PHP

```bash
cd healthcare-simple
php -S localhost:8000
```

### Вариант 4: Node.js

```bash
cd healthcare-simple
npx http-server -p 8000
```

## 📝 Первые шаги

### 1. Измените цвета сайта

Откройте [css/variables.css](css/variables.css):

```css
:root {
    --color-primary: #0693e3;    /* Ваш основной цвет */
    --color-secondary: #2c3e50;  /* Ваш вторичный цвет */
}
```

### 2. Замените логотип

Замените файл [images/logo.svg](images/logo.svg) на ваш логотип.

### 3. Обновите контент

Откройте [js/i18n_categorized.json](js/i18n_categorized.json) и измените тексты:

```json
{
  "content_en": {
    "hero_title": {
      "ko": "Ваш заголовок",
      "en": "Your Title",
      "zh-tw": "您的標題"
    }
  }
}
```

### 4. Добавьте свои изображения

Поместите изображения в папку `images/` и используйте в HTML:

```html
<img src="images/your-image.jpg" alt="Description">
```

### 5. Создайте новую страницу

```bash
# Скопируйте шаблон
cp pages/contact.html pages/new-page.html

# Отредактируйте содержимое
# Добавьте ссылку в навигацию
```

## 🎨 Кастомизация

### Изменить шрифты

В [css/reset.css](css/reset.css):

```css
body {
    font-family: 'Your Font', sans-serif;
}
```

### Добавить Google Fonts

В `index.html`:

```html
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
```

### Изменить Hero фон

В [css/main.css](css/main.css):

```css
.hero {
    background: linear-gradient(135deg, #your-color1 0%, #your-color2 100%);
}
```

## 📱 Тестирование

### Проверка на разных устройствах:

1. **Desktop**: Откройте в браузере
2. **Mobile**: Откройте DevTools (F12) → Toggle device toolbar
3. **Tablet**: Измените размер окна

### Проверка переводов:

1. Кликните на переключатель языка
2. Выберите язык
3. Проверьте, что весь контент переведен

## 🐛 Решение проблем

### Не работает переключатель языков?

1. Проверьте консоль браузера (F12)
2. Убедитесь что файл `js/i18n_categorized.json` загружается
3. Проверьте, что сервер запущен (не просто открыт файл)

### Не загружаются стили?

1. Проверьте пути к CSS файлам
2. Убедитесь что файлы существуют
3. Проверьте консоль на ошибки 404

### Изображения не отображаются?

1. Проверьте путь к изображению
2. Убедитесь что файл существует
3. Используйте placeholder: `onerror="this.src='images/placeholder.svg'"`

## 📚 Дополнительная информация

- [README.md](README.md) - Полная документация
- [MIGRATION_GUIDE.md](MIGRATION_GUIDE.md) - Руководство по миграции с WordPress

## ✅ Checklist готовности к продакшену

- [ ] Заменил все placeholder изображения
- [ ] Обновил все тексты в i18n
- [ ] Изменил цвета на фирменные
- [ ] Добавил свой логотип
- [ ] Протестировал на мобильных
- [ ] Проверил все ссылки
- [ ] Настроил формы
- [ ] Добавил аналитику
- [ ] Создал sitemap.xml
- [ ] Настроил SEO мета-теги

## 🎉 Готово к деплою!

```bash
# GitHub Pages
git push origin main

# Netlify
netlify deploy --prod

# Vercel
vercel --prod
```

---

**Приятной работы! 💙**
