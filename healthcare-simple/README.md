# Healthcare Website - Simple HTML/CSS/JS Version

Простая, современная версия healthcare website без WordPress зависимостей. Чистый HTML, CSS и JavaScript.

## 🎯 Особенности

- ✅ **Чистый код**: Только HTML, CSS, JavaScript - никаких фреймворков
- ✅ **Многоязычность**: Полная поддержка i18n (Korean, English, Chinese)
- ✅ **Адаптивный дизайн**: Работает на всех устройствах
- ✅ **Современный UI**: CSS Grid, Flexbox, анимации
- ✅ **Быстрая загрузка**: Оптимизированный код
- ✅ **SEO-friendly**: Семантическая разметка
- ✅ **Легко поддерживать**: Простая структура

## 📁 Структура проекта

```
healthcare-simple/
├── index.html              # Главная страница
├── css/
│   ├── reset.css          # CSS reset
│   ├── variables.css      # CSS переменные (цвета, размеры)
│   ├── main.css           # Основные стили
│   ├── header.css         # Стили хедера
│   ├── footer.css         # Стили футера
│   └── responsive.css     # Медиа-запросы
├── js/
│   ├── i18n-loader.js     # Система переводов
│   ├── i18n_categorized.json  # Данные переводов
│   ├── lang-switcher.js   # Переключатель языков
│   ├── main.js            # Основная логика
│   └── navigation.js      # Навигация
├── pages/
│   ├── contact.html       # Страница контактов
│   ├── portfolio.html     # Портфолио
│   ├── research.html      # Исследования
│   └── faq.html          # FAQ
├── images/                # Изображения
├── assets/                # Дополнительные ресурсы
│   ├── fonts/            # Шрифты
│   └── icons/            # Иконки
└── README.md             # Этот файл
```

## 🚀 Быстрый старт

### 1. Запуск локально

**Вариант A: Python HTTP Server**
```bash
cd healthcare-simple
python3 -m http.server 8000
```

**Вариант B: Node.js HTTP Server**
```bash
cd healthcare-simple
npx http-server -p 8000
```

**Вариант C: PHP Built-in Server**
```bash
cd healthcare-simple
php -S localhost:8000
```

Затем откройте браузер: `http://localhost:8000`

### 2. Просто открыть файл

Можно просто открыть `index.html` в браузере, но некоторые функции (например, fetch для JSON) могут не работать из-за CORS.

## 🌐 Система i18n

Проект использует собственную систему интернационализации.

### Поддерживаемые языки:
- 🇰🇷 Корейский (ko) - по умолчанию
- 🇺🇸 Английский (en)
- 🇹🇼 Традиционный китайский (zh-tw)

### Как это работает:

1. **В HTML используйте data-атрибуты:**
```html
<!-- Обычный текст -->
<h1 data-i18n="navigation.home">Home</h1>

<!-- Placeholder -->
<input data-i18n-placeholder="forms.email" placeholder="Email">

<!-- Alt текст -->
<img data-i18n-alt="content_en.logo" alt="Logo">

<!-- ARIA labels -->
<button data-i18n-aria="navigation.menu" aria-label="Menu">☰</button>
```

2. **Переводы хранятся в JSON:**
```json
{
  "navigation": {
    "home": {
      "ko": "홈",
      "en": "Home",
      "zh-tw": "首頁"
    }
  }
}
```

3. **Автоматическое применение:**
- При загрузке страницы
- При смене языка
- При динамическом добавлении контента

### Добавление новых переводов:

1. Откройте `js/i18n_categorized.json`
2. Добавьте новую строку в нужную категорию:
```json
{
  "buttons": {
    "new_button": {
      "ko": "새 버튼",
      "en": "New Button",
      "zh-tw": "新按鈕"
    }
  }
}
```
3. Используйте в HTML:
```html
<button data-i18n="buttons.new_button">New Button</button>
```

## 🎨 Кастомизация

### Изменение цветов:

Откройте `css/variables.css` и измените переменные:

```css
:root {
    --color-primary: #0693e3;      /* Основной цвет */
    --color-secondary: #2c3e50;    /* Вторичный цвет */
    --color-accent: #e74c3c;       /* Акцентный цвет */
}
```

### Изменение типографики:

```css
:root {
    --font-size-base: 1rem;        /* Базовый размер */
    --font-size-xl: 1.25rem;       /* Большой */
    --font-size-2xl: 1.5rem;       /* Очень большой */
}
```

### Изменение отступов:

```css
:root {
    --spacing-sm: 1rem;            /* Малый */
    --spacing-md: 1.5rem;          /* Средний */
    --spacing-lg: 2rem;            /* Большой */
}
```

## 📱 Адаптивность

Проект адаптируется под все экраны:

- 📱 **Mobile**: < 768px
- 💻 **Tablet**: 768px - 1024px
- 🖥️ **Desktop**: > 1024px

Брейкпоинты настраиваются в `css/responsive.css`.

## 🔧 Основные компоненты

### Header (Хедер)
- Логотип
- Навигационное меню
- Выпадающие подменю
- Переключатель языков
- Мобильное меню (гамбургер)

### Footer (Футер)
- Информационные колонки
- Ссылки
- Социальные сети
- Copyright

### Hero Section (Главный баннер)
- Градиентный фон
- Анимация появления
- Call-to-action кнопка

### Features (Функции/Услуги)
- Сетка карточек
- Иконки Font Awesome
- Hover эффекты

### News (Новости)
- Адаптивная сетка
- Изображения с lazy loading
- Карточки с информацией

## 📝 Создание новой страницы

1. Скопируйте `pages/contact.html`
2. Переименуйте в нужное название
3. Измените content внутри `<main>`
4. Добавьте ссылку в навигацию

Пример:
```html
<li><a href="pages/new-page.html" data-i18n="navigation.new_page">New Page</a></li>
```

## 🎯 JavaScript API

### Main App
```javascript
// Форматирование даты
App.formatDate('2024-01-15'); // Returns: "January 15, 2024"

// Загрузка новостей
App.loadNews();
```

### Navigation
```javascript
// Подсветка активной страницы
Navigation.highlightActivePage();
```

### Language Switcher
```javascript
// Программная смена языка
const switcher = new LanguageSwitcher('lang-switcher');
switcher.switchLanguage('en');
```

## 🔍 SEO Оптимизация

Для лучшего SEO:

1. **Добавьте meta теги:**
```html
<meta name="description" content="Описание страницы">
<meta name="keywords" content="ключевые, слова">
```

2. **Используйте семантические теги:**
```html
<article>, <section>, <nav>, <header>, <footer>
```

3. **Добавьте Open Graph:**
```html
<meta property="og:title" content="Заголовок">
<meta property="og:description" content="Описание">
<meta property="og:image" content="image.jpg">
```

## 📦 Деплой

### GitHub Pages:
1. Загрузите в репозиторий
2. Settings → Pages → Source: main branch
3. Готово!

### Netlify:
1. Перетащите папку на netlify.com
2. Или подключите GitHub репозиторий
3. Автоматический деплой при push

### Vercel:
```bash
npm i -g vercel
cd healthcare-simple
vercel
```

### Обычный хостинг:
1. Загрузите все файлы по FTP
2. Убедитесь что `index.html` в корне
3. Настройте .htaccess при необходимости

## 🛠️ Возможные улучшения

- [ ] Добавить форму обратной связи с backend
- [ ] Интегрировать Google Maps
- [ ] Добавить галерею с lightbox
- [ ] Создать блог-систему
- [ ] Добавить поиск по сайту
- [ ] Интегрировать аналитику (Google Analytics)
- [ ] Добавить PWA функционал
- [ ] Создать админ-панель для контента

## 📄 Лицензия

MIT License - используйте свободно для любых целей.

## 👥 Поддержка

Если возникли вопросы:
1. Проверьте документацию
2. Посмотрите примеры в коде
3. Откройте issue на GitHub

## 🔄 Миграция с WordPress

Этот проект - упрощенная версия WordPress сайта:

**Что сохранено:**
- Вся система i18n (483 строки переводов)
- Структура навигации
- Основные страницы
- Дизайн и стили

**Что убрано:**
- WordPress зависимости
- PHP backend
- База данных
- Плагины

**Преимущества:**
- ⚡ Быстрее загрузка (нет WP overhead)
- 🔒 Безопаснее (нет PHP уязвимостей)
- 💰 Дешевле хостинг (статика)
- 🛠️ Проще поддержка (обычный код)

---

**Сделано с ❤️ для простоты и производительности**
