# 🚀 Руководство по интеграции системы i18n

## 📋 Быстрый старт (5 минут)

### Шаг 1: Подключите скрипты

Добавьте эти строки **перед закрывающим тегом `</body>`** в ваш HTML:

```html
<!-- i18n System -->
<script src="wp-content/i18n-full.js"></script>
<script src="wp-content/i18n-replacer-v2.js"></script>
```

**Важно!** Порядок подключения имеет значение:
1. Сначала `i18n-full.js` (содержит переводы)
2. Затем `i18n-replacer-v2.js` (делает замену текстов)

### Шаг 2: Добавьте переключатель языков

Скопируйте содержимое файла `language-switcher.html` и вставьте в header вашего сайта:

```html
<head>
  <!-- Ваши мета-теги и стили -->

  <!-- Стили и компонент переключателя языков -->
  <!-- Содержимое из wp-content/language-switcher.html -->
</head>
```

### Шаг 3: Откройте сайт в браузере

Теперь переключатель языков должен работать! Нажмите на кнопки, чтобы переключить язык.

---

## 🧪 Тестирование

### Тестовая страница

Откройте файл `test-i18n.html` в браузере:

```bash
open test-i18n.html
# или
file:///Users/meditor/Projects/healthcare-web/test-i18n.html
```

Эта страница демонстрирует все возможности системы:
- ✓ Автоматическое определение языка браузера
- ✓ Сохранение выбранного языка в localStorage
- ✓ Замена всех корейских текстов
- ✓ Красивая анимация при переключении
- ✓ Индикация текущего языка

### Проверочный список

- [ ] Переключатель языков отображается
- [ ] При клике на "English" тексты меняются на английский
- [ ] При клике на "繁體中文" тексты меняются на традиционный китайский
- [ ] При клике на "한국어" тексты возвращаются на корейский
- [ ] Активная кнопка подсвечена
- [ ] После перезагрузки страницы язык сохраняется

---

## 🔧 Интеграция в существующие страницы

### Вариант 1: Минимальная интеграция

Просто добавьте скрипты в конец `<body>`:

```html
<body>
  <!-- Ваш существующий контент -->

  <!-- i18n System -->
  <script src="wp-content/i18n-full.js"></script>
  <script src="wp-content/i18n-replacer-v2.js"></script>
</body>
```

### Вариант 2: Полная интеграция с переключателем

```html
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>Ваш сайт</title>

    <!-- Стили переключателя -->
    <style>
        .language-switcher {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 30px;
            padding: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            display: flex;
            gap: 6px;
        }

        .language-switcher button {
            background: transparent;
            border: none;
            padding: 10px 16px;
            font-size: 14px;
            font-weight: 500;
            color: #666;
            cursor: pointer;
            border-radius: 20px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .language-switcher button::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            opacity: 0;
            transition: opacity 0.3s ease;
            border-radius: 20px;
        }

        .language-switcher button span {
            position: relative;
            z-index: 1;
        }

        .language-switcher button.active {
            color: white;
            font-weight: 600;
        }

        .language-switcher button.active::before {
            opacity: 1;
        }

        .language-switcher button .flag {
            margin-right: 6px;
            font-size: 16px;
        }
    </style>
</head>
<body>

    <!-- Переключатель языков -->
    <div class="language-switcher">
        <button onclick="changeLanguage('ko')" data-lang="ko" class="active">
            <span class="flag">🇰🇷</span>
            <span>한국어</span>
        </button>
        <button onclick="changeLanguage('en')" data-lang="en">
            <span class="flag">🇬🇧</span>
            <span>English</span>
        </button>
        <button onclick="changeLanguage('zh')" data-lang="zh">
            <span class="flag">🇹🇼</span>
            <span>繁體中文</span>
        </button>
    </div>

    <!-- Ваш контент -->
    <h1>건강과 과학의 조화를 통해 만들어온 혁신의 여정</h1>
    <p>2010년 설립된 전문 연구기관으로...</p>

    <!-- i18n System -->
    <script src="wp-content/i18n-full.js"></script>
    <script src="wp-content/i18n-replacer-v2.js"></script>

</body>
</html>
```

---

## 🎨 Кастомизация переключателя

### Изменить позицию

```css
.language-switcher {
    top: 20px;     /* Расстояние сверху */
    right: 20px;   /* Расстояние справа */
    /* Или используйте left, bottom */
}
```

### Изменить цвета

```css
.language-switcher button.active::before {
    background: linear-gradient(135deg, #your-color-1 0%, #your-color-2 100%);
}
```

### Компактный вариант (только флаги)

```html
<div class="language-switcher">
    <button onclick="changeLanguage('ko')" data-lang="ko">🇰🇷</button>
    <button onclick="changeLanguage('en')" data-lang="en">🇬🇧</button>
    <button onclick="changeLanguage('zh')" data-lang="zh">🇹🇼</button>
</div>
```

### Вертикальный вариант

```css
.language-switcher {
    flex-direction: column;
}
```

---

## 📱 Адаптивность

Переключатель автоматически адаптируется под мобильные устройства:

```css
@media (max-width: 768px) {
  .language-switcher {
    top: 10px;
    right: 10px;
    padding: 6px;
  }

  .language-switcher button {
    padding: 8px 12px;
    font-size: 13px;
  }
}
```

---

## 🔍 Как это работает

### 1. Автоматическое определение текстов

`i18n-replacer-v2.js` автоматически находит все корейские тексты на странице:

```javascript
// Маппинг корейский текст → ключ перевода
'암 연구' → 'research.text1'
'의료기관' → 'projects.text4'
```

### 2. Замена текстов

При переключении языка:

```javascript
changeLanguage('en') // Переключаемся на английский
↓
'암 연구' → 'Cancer Research'
'의료기관' → 'Medical Institution'
```

### 3. Сохранение выбора

Выбранный язык сохраняется в `localStorage`:

```javascript
localStorage.setItem('selectedLanguage', 'en');
```

При следующем посещении сайта язык восстановится автоматически.

---

## 🛠️ API для разработчиков

### Программное переключение языка

```javascript
// Переключить на английский
changeLanguage('en');

// Получить текущий язык
i18nReplacer.getCurrentLanguage(); // 'en'

// Получить список поддерживаемых языков
i18nReplacer.getSupportedLanguages(); // ['ko', 'en', 'zh']

// Принудительная замена всех текстов
i18nReplacer.replaceAllTexts();
```

### Получение переводов в JavaScript

```javascript
// Получить перевод по ключу
const text = i18nFull.t('history.text1');
console.log(text); // "건강과 과학의 조화..." (на корейском)

// После смены языка
changeLanguage('en');
const textEn = i18nFull.t('history.text1');
console.log(textEn); // "The Journey of Innovation..." (на английском)
```

### События

```javascript
// Добавить обработчик смены языка
const originalChange = window.changeLanguage;
window.changeLanguage = function(lang) {
    // Ваш код перед сменой языка
    console.log('Меняем язык на:', lang);

    // Вызываем оригинальную функцию
    const result = originalChange(lang);

    // Ваш код после смены языка
    console.log('Язык изменен!');

    return result;
};
```

---

## 🐛 Отладка

### Консольные сообщения

Система выводит полезную информацию в консоль браузера (F12):

```
🚀 i18n-replacer-v2 инициализирован
📌 Восстановлен сохраненный язык: en
🌐 Переключение языка на: ru
🔄 Начинаю замену текстов...
✅ Замена завершена на язык: ru
```

### Проверка загрузки

Откройте консоль (F12) и выполните:

```javascript
// Проверить наличие i18nFull
typeof i18nFull !== 'undefined' // должно быть true

// Проверить наличие changeLanguage
typeof changeLanguage === 'function' // должно быть true

// Посмотреть текущий язык
i18nFull.currentLanguage // 'ko', 'en' или 'zh'

// Получить статистику
i18nFull.getStats()
// Вернет количество текстов для каждого языка
```

### Частые проблемы

**Проблема:** Тексты не меняются

**Решение:**
1. Проверьте порядок подключения скриптов
2. Откройте консоль браузера (F12) и проверьте ошибки
3. Убедитесь что скрипты загружены: `console.log(typeof i18nFull)`

**Проблема:** Некоторые тексты не переводятся

**Решение:**
1. Проверьте, есть ли этот текст в `textToKeyMap` (i18n-replacer-v2.js)
2. Если текста нет, добавьте его в маппинг
3. Убедитесь что текст точно совпадает (пробелы важны!)

**Проблема:** Переключатель не отображается

**Решение:**
1. Проверьте что CSS стили загружены
2. Проверьте z-index (должен быть 9999)
3. Проверьте что кнопки имеют `onclick="changeLanguage('...')"`

---

## 📊 Поддерживаемые тексты

Система автоматически переводит следующие категории:

- ✅ **Footer** (16 текстов): контактная информация
- ✅ **History** (11 текстов): история компании
- ✅ **Overview** (9 текстов): о компании
- ✅ **Projects** (76 текстов): проекты и кейсы
- ✅ **Research** (38 текстов): исследования
- ✅ **News** (41 текст): новости и анонсы
- ✅ **FAQ** (5 текстов): вопросы-ответы
- ✅ **Location** (5 текстов): местоположение
- ✅ **Contact** (9 текстов): контакты
- ✅ **Buttons** (3 текста): кнопки
- ✅ **Categories** (3 текста): категории

**Всего: 216 уникальных корейских текстов**

---

## 🔄 Добавление новых переводов

### 1. Найдите новый корейский текст

Например: `새로운 텍스트`

### 2. Добавьте в i18n-full.js

```javascript
ko: {
  common: {
    // ... существующие
    newText1: '새로운 텍스트'
  }
}

en: {
  common: {
    // ... существующие
    newText1: 'New Text'
  }
}

ru: {
  common: {
    // ... существующие
    newText1: 'Новый текст'
  }
}
```

### 3. Добавьте в i18n-replacer-v2.js

```javascript
const textToKeyMap = {
  // ... существующие
  '새로운 텍스트': 'common.newText1',
};
```

### 4. Готово!

Теперь этот текст будет автоматически переводиться.

---

## 📚 Дополнительные ресурсы

- `test-i18n.html` - Демонстрация всех возможностей
- `TEXT_ANALYSIS_SUMMARY.md` - Полный отчет по анализу
- `texts-catalog.csv` - Каталог всех текстов для Excel
- `I18N_README.md` - Базовая документация

---

## ✅ Чеклист интеграции

- [ ] Скопировал `i18n-full.js` в проект
- [ ] Скопировал `i18n-replacer-v2.js` в проект
- [ ] Подключил скрипты в HTML (правильный порядок!)
- [ ] Добавил переключатель языков
- [ ] Протестировал на test-i18n.html
- [ ] Проверил работу на реальных страницах
- [ ] Настроил стили под дизайн сайта
- [ ] Проверил на мобильных устройствах
- [ ] Проверил сохранение языка после перезагрузки

---

## 🎉 Готово!

Ваша система интернационализации готова к работе!

**Нужна помощь?** Проверьте консоль браузера или обратитесь к документации.
